#include "GameEntity.h"

GameEntity::GameEntity(const double& health, const double& health_regen, const double& max_health,const int& size,const int& vx, const int& vy,const double& xp):
health(health), health_regen(health_regen), max_health(max_health), alive(alive), size(size), vx(vx), vy(vy), xp(xp) {}

